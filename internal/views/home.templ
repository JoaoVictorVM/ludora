package views

// Home is a placeholder page so the shared layout has a rendered surface before
// F06 implements the real feed. F06 replaces its contents.
templ Home() {
	@Layout("Ludora") {
		<h1 class="text-2xl font-semibold">Ludora</h1>
		<p class="mt-2 text-muted">Avalie e comente jogos, sem cadastro.</p>
	}
}
